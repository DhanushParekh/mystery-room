// Copy the entire React component code from my previous response here, 
// but remove the script tags and Babel type declaration
const { useState, useEffect } = React;

// Rest of the MysteryRoom component code...

// Render the app
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<MysteryRoom />);
